1. h2 DB 설치를 한다.
2. DB에 테이블을 Create 한다
drop table if exists member CASCADE;
create table member
(
 id bigint generated by default as identity,
 name varchar(255),
 primary key (id)
);


3. 환경 설정 및 h2 접근 방법은
스프링 입문 - 코드로 배우는 스프링 필기 문서를 참고한다
